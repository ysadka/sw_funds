Show User's Trips
</br>
</br>

<% @trips.each do |trip| %>

<%= trip.first_name %>
<%= trip.last_name %>
<%= trip.confirmation_code %>
<%= trip.funds_remaining %>
<%= trip.exp_date %>
<%= link_to "Edit Trip", edit_trip_path(trip.id) %>
<%= link_to "Delete Trip", trip_path, method: :delete %> 
</br>

<% end %>

</br>
</br>

<%= link_to "Add New Trip", new_trip_path %>