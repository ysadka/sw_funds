Show User's Trips

<% @trips.each do |trip| %>

<%= trip.first_name %>
<%= trip.last_name %>
<%= trip.confirmation_code %>
<%= trip.funds_remaining %>
<%= trip.exp_date %>
<%= link_to edit_trip_path %>
<%= link_to trip_path(:destroy) %>

<% end %>

<%= link_to "Add New Trip", new_trip_path %>